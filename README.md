# e确镜 · 接口服务

Express + PostgreSQL + JWT。与 `e-confirm-admin`、`e-confirm-app` 并列。

部署目录（服务器）：

- API：`/var/www/econfirm/api`
- 管理端静态页：`/var/www/econfirm/web`

本机开发：

```bash
cp .env.example .env
npm install
npm run dev
```

默认账号 `admin` / `123456`。
